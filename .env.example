# Optional: authoritative fixture refresh via API-Football (api-sports.io).
# Copy to `.env` and fill in. This key is used ONLY by scripts/fetch-fixtures.mjs
# at build time and NEVER ships to the client bundle. Leave unset to use the
# verified seed generator instead.
API_FOOTBALL_KEY=
